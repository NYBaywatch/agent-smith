# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
/bin/
/dist/
# The built binary at the repo root only — anchored with a leading slash so it
# does NOT match the cmd/agent-smith/ source directory.
/agent-smith
/agent-smith.exe

# Test / coverage
*.test
*.out
coverage.*

# Go workspace
go.work
go.work.sum

# Build / manifest temp — but DO track the committed Windows resource so the
# GUI (app manifest: Common Controls v6 + DPI awareness) builds in CI.
*.syso
!cmd/agent-smith/rsrc_windows.syso

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Local config / data written at runtime
*.local
/data/
*.db
*.sqlite
